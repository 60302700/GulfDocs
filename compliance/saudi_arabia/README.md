Saudi Arabia compliance module (skeleton)

- Place versioned rule directories here, e.g. `2026-01/`, `2026-07/`.
- Each version should include `rules.yaml` and `schema.json` describing required fields and exports (ZATCA XML mapping).

Example structure:

saudi_arabia/
2026-01/
rules.yaml
schema.json
tests/
